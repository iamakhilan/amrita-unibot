streamlit==1.28.2
requests==2.31.0
python-dotenv==1.0.0
chromadb==0.3.32
tensorflow==2.14.1

# Optional / platform-specific (uncomment if needed)
# tensorflow-cpu==2.14.1  # use CPU-only build on machines without GPU